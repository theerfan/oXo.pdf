{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Viewer.html",
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "url": "http://localhost:8000/viewer.html", // Adjust the URL if needed
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start Node Server",
            "postDebugTask": "Terminate Node Server"
        }
    ]
}
